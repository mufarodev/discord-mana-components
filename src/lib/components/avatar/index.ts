import Root, { avatarVariants, type AvatarProps, type AvatarSize, type AvatarStatus } from './avatar.svelte';

export {
	Root,
	Root as Avatar,
	avatarVariants,
	type AvatarProps,
	type AvatarSize,
	type AvatarStatus
};
