import Root, {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonStatic,
	type ButtonVariant
} from './button.svelte';

export {
	Root,
	type ButtonProps as Props,
	Root as Button,
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonStatic,
	type ButtonVariant
};
